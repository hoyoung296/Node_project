<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>마이페이지</title>
</head>
<body>
    <h1>마이페이지</h1>
    <p>안녕하세요, <%= user && user.NAME ? user.NAME : '' %>님!</p>
    <p>아이디: <%= user && user.ID ? user.ID : '' %></p>
    <p>이메일: <%= user && user.EMAIL ? user.EMAIL : '' %></p>
    
    <div>
        <h2>프로필</h2>
        <img src="<%= user.picture ? '/uploads/' + user.picture : '/default-profile.png' %>" alt="Profile Picture" width="150">
        <p><%= user.msg ? user.msg : '상태 메시지가 없습니다.' %></p>
        <p><a href="/mypage/profile">프로필 설정</a></p>
    </div>

    <h2>내정보 관리</h2>
    <ul>
        <li><a href="/mypage/edit">내정보 수정</a></li>
        <li><a href="/mypage/delete">회원탈퇴</a></li>
        <!-- 나호영 친구 추가 기능 알람 목록 및 친구 목록 확인 경로 추가 -->
        <li><a href="/friends/alram">알람 확인</a></li>
        <li><a href="/friends/friendsview">친구 확인</a></li>
    </ul>
    
    <form action="/mypage/logout" method="POST">
        <button type="submit">로그아웃</button>
    </form>
    <a href="/">메인페이지</a>
</body>
</html>