<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/static/css/friends/view1.css" rel="stylesheet">
    <script src="/static/js/top.js"></script> <!-- 사용자thema설정을 위한 js파일 -->
    <script>
        const thema = "<%= thema %>" //사용자 thema변수 저장
        window.onload = () => {
            applyThema(thema) //사용자 thema 설정
        }
    </script>
</head>

<body>
    <div id="main">
        <table align="center" width="730px" height="300px" border="1">
            <tr style="border-top: 10px solid var(--header-color);">
                <th>번호</th>
                <td>
                    <%= result[0].NUM %>
                </td>
                <th>보낸 사람</th>
                <td>
                    <%= result[0].SENDER %>
                </td>
            </tr>
            <tr>
                <th>받는 사람</th>
                <td>
                    <%= result[0].RECEIVER %>
                </td>
                <th>목적</th>
                <td>
                    <%= result[0].OBJECT %>
                </td>
            </tr>
            <tr>
                <th>제목</th>
                <td>
                    <%= result[0].SUBJECT %>
                </td>
                <th>내용</th>
                <td>
                    <%= result[0].CONTENT %>
                </td>
            </tr>
            <tr style="border-bottom: 10px solid var(--header-color);">
                <th colspan="2">보낸 시간</th>
                <td colspan="2">
                    <%= result[0].SAVE_DATE %>
                </td>
            </tr>
        </table>
        <% if(result[0].OBJECT=="친구 추가" && result[0].RECEIVER==name) { %>
            <form action="/friends/update" method="post" id="form1">
                <input type="hidden" name="mem_id" value="<%= result[0].RECEIVER %>">
                <input type="hidden" name="fr_id" value="<%= result[0].SENDER %>">
                <button type="submit">친구추가</button>
            </form>
            <% } %>
                <form action="/friends/del" method="post" id="form2">
                    <input type="hidden" name="num" value="<%= result[0].NUM %>">
                    <button type="submit">알림 삭제</button>
                </form>
                <button id="button3" onclick="location.href='/friends/alram'">목록페이지</button>
    </div>
</body>

</html>