<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DOTORI</title>
    <link rel="icon" href="/static/front_image/dotoree.png"image/x-icon">
    <style>
        body{
            background-color: #fff1bd;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        #dotoree{
            background-image: url(/static/front_image/dotoree.png);
            background-size: contain;
            width: 80px;
            height: 100px;
            display: flex;
            order: 1;
            border: none;
            cursor: pointer;
            background-color: transparent;
        }

        #dotoree:hover{
            animation: dotoreee 0.2s infinite;
        }
        @keyframes dotoreee{
            0% {
                transform: rotate(-10deg);
            }
            50% {
                transform: rotate(10deg);
            }
            100% {
                transform: rotate(-10deg);
            }
        }
       

    </style>
</head>
<body>
    <div>
        <input type="button" id="dotoree" onclick="location.href='/main'">
    </div>
</body>
</html>